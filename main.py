# -*- coding: utf-8 -*-
"""crop prediction model

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1yUBMHZA4GnDaK1AijnoCeSLTZe75-ChI
"""

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

import pandas as pd
import os
from google.colab import files

file_path = "/content/Crop_recommendation.csv"

# Check if the file exists, if not, prompt for upload
if not os.path.exists(file_path):
    print(f"'{file_path}' not found. Please upload the 'Crop_recommendation.csv' file:")
    try:
        uploaded = files.upload()
        if 'Crop_recommendation.csv' in uploaded:
            print(f"'{file_path}' uploaded successfully.")
        else:
            print("Upload cancelled or incorrect file name provided. Please make sure to upload 'Crop_recommendation.csv'.")
            # You might want to handle this case more robustly, e.g., by exiting or raising an error
    except Exception as e:
        print(f"An error occurred during file upload: {e}")

# Attempt to read the CSV file
try:
    data = pd.read_csv(file_path)
    print("File read successfully.")
except FileNotFoundError:
    print(f"Error: The file '{file_path}' was still not found after attempting to ensure its presence. Please check the file's location or try uploading again.")
except Exception as e:
    print(f"An unexpected error occurred while reading the file: {e}")

data.head()

data.shape

data.isnull().sum()

x=data.iloc[:,:-1] #features
y=data.iloc[:,-1] #label

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.2,random_state=42)

model=RandomForestClassifier()

model.fit(x_train,y_train)

prediction=model.predict(x_test)

accuracy=model.score(x_test,y_test)

print("accurate_valu:", accuracy)

data.tail()

x_train.head()

x_train.shape

x_test.head()

y_test.head()

y_train.shape

y_train.shape

y_train.head()

predicted_crop=model.predict([[17,	16,	14,	16.396243,	92.181519	,6.625539,	102.944161]])

predicted_crop=model.predict(x_train)
print("predicted crop:", predicted_crop[1179])

new_features=[[5,14,0.999,7.88,4.988,5.9777,89.67]]
predicted_crop=model.predict(new_features)
print("predicted_crop:",predicted_crop)